export interface LaravelPaginationWrapper<DataT> {
  data: DataT[];
  meta: {
    current_page: number;
    from: number | null;
    last_page: number;
    path: string;
    per_page: number;
    to: number | null;
    total: number;
  };
}
